fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
pydantic==2.10.3
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Pin: passlib 1.7.4 (unmaintained) probes bcrypt internals in a way that
# raises ValueError on bcrypt>=4.1 (which rejects >72-byte test strings
# instead of truncating). Keep bcrypt pinned below that until passlib is
# updated/replaced.
bcrypt==4.0.1
python-multipart==0.0.20
email-validator==2.2.0
gunicorn==23.0.0
